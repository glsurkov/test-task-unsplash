import { Button, ButtonList } from './ui/buttons';
import { Root } from './ui/root';

export { MODAL_CLOSE_DELAY_IN_MS } from './ui/root';

export const Modal = {
    Root: Root,
    ButtonList: ButtonList,
    Button: Button,
};
